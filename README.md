# ElectionBoothSystem
Election Booth Management System is a Spring Boot MVC application using JPA and MySQL to manage booths, register voters, track voting status, and monitor turnout. It follows layered architecture with REST APIs, exception handling, DTO, and CORS config. APIs were tested with Postman and a simple HTML UI enables basic operations.
